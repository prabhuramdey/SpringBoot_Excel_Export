spring:
  application:
    name: 02-SpringBoot_Excel_Export
  datasource:
    driver: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/sbms
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  mvc:
    pathmatch:
      matching-strategy: ant-path-matcher
